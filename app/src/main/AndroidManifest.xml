<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.mradziewicz.savemoneyaccount">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaveMoneyAccount">
        <activity android:name=".AddNewGroupExpenses"></activity>
<!--        <activity-->
<!--            android:name=".AddNewExpenseActivity"-->
<!--            android:label="@string/title_activity_add_new_expense"-->
<!--            android:theme="@style/Theme.SaveMoneyAccount.NoActionBar" />-->
        <activity android:name=".AddCostActivity" />
<!--        <activity android:name=".ExpensesActivity" />-->
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Expense"/>
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>